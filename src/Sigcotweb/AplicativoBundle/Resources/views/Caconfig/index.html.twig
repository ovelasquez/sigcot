{% extends '::base.html.twig' %}

{% block content %}
<h1>Caconfig list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Porcentajemax</th>
            <th>Interes</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('configcajadeahorro_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.porcentajemax }}</td>
            <td>{{ entity.interes }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('configcajadeahorro_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('configcajadeahorro_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('configcajadeahorro_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}
{% block submenu %}
{% include 'AplicativoBundle:Caprestamos:submenu.html.twig' %}
{% endblock %}